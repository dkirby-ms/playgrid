# Orchestration: Marathe — Infrastructure & CI Updates

**Timestamp:** 2026-03-20T12:17:35Z  
**Agent:** Marathe (DevOps/CI-CD)  
**Status:** Implementation Completed

## Work Summary

Added environment-driven game availability control to infrastructure and deployment pipelines.

**Key Outputs:**
- `infra/main.bicep` — new `disabledGames` variable, conditional on `environmentName`. Prod: `risk` disabled. Dev/UAT: empty (all enabled).
- `deploy-prod.yml` — `DISABLED_GAMES=risk` added to `--set-env-vars`
- Container start command switched from placeholder HTTP server to actual app: `node server/dist/src/index.js`
- Bug fix: `set-repo-secrets.sh` — corrected prod `CONTAINER_APP_NAME` from `playgrid-uat` to `playgrid-prod`

**Impact:**
- Prod deploys now disable Risk automatically
- Server config module reads `DISABLED_GAMES` env var and filters registry at startup
- Bootstrap container finally runs the real app (CI overrides image from ACR anyway)

**Known Limitation:**
- `set-repo-secrets.sh` fix doesn't propagate via git (file is gitignored). Manual verification required on new machines.

**Status:** Implementation complete. Deploys will enforce game availability per environment.
