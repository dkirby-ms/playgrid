# Orchestration Log: Gately (Game Dev)

**Date:** 2026-03-22T14:12:19Z  
**Task:** Fix stale lobby room state — "Leave your current game" error  
**Mode:** background  
**Outcome:** ✅ SUCCESS

## Summary

Fixed players seeing "Leave your current game before creating another" after already leaving. The lobby's `currentGameId` was not being cleared because `LEAVE_GAME` messages were only sent from the waiting-room UI, not from game-room exit paths.

## Changes

- **Application.ts** — Added `notifyLobbyGameLeft()` call whenever player exits a game room (all exit paths)
- **SetupScreen.ts** — Calls `notifyLobbyGameLeft()` on leave
- **SetupScene.ts** — Calls `notifyLobbyGameLeft()` on cleanup
- **LobbyRoom.ts** — Added `clearStaleGameAssignment()` server-side validation; called at top of `handleCreateGame()` and `handleJoinGame()`

## Tests

Added 4 regression tests covering:
- Player leave triggers lobby notification
- Stale game reference is cleared on create attempt
- Stale game reference is cleared on join attempt
- Double-clear is idempotent

## Validation

- ✅ Build passes
- ✅ Lint passes
- ✅ 803 tests pass (4 new regression tests added)

## Decision Recorded

See `.squad/decisions/inbox/gately-stale-lobby.md` — convention: all scene classes managing lobby state must implement `cleanup()` and call it from `onExit()`.
