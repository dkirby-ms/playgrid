# Orchestration Log: Gately (Game Dev)
**Session:** P6.4 Polish — Move History final polish pass  
**Timestamp:** 2026-03-19T19:17:12Z

## Work Completed

**Task:** Fixed Dominos stat bug + enriched Risk stats + structured detail rendering

### Fixes & Features
1. **Dominos stat bug** — HistoryScreen was checking `payload.action` (undefined) instead of `entry.actionType` for determining move type. Result: zero stat counts. Fixed by reading correct field.

2. **Risk stats enrichment** — Extended Risk formatter to track reinforcements, territory picks, and card trades as distinct move types (not just "move"). Stats now report accurate counts per action type.

3. **MoveFormatter interface extension** — Added `formatMoveDetails(entry): MoveDetailItem[]` to return structured label/value pairs for expanded move details. Each formatter returns game-specific content:
   - Checkers: source/dest coordinates + piece type
   - Backgammon: movement details + dice
   - Dominos: played tile + pip count
   - Risk: reinforcements, territory, cards, bonus
   - Default fallback: raw payload iteration for unknown games

### Implementation Details
- `client/src/ui/historyFormatters.ts` — Added `MoveDetailItem` type, `formatMoveDetails` method to all formatters
- `client/src/ui/HistoryScreen.ts` — Fixed actionType bugs in dominos/risk stats, replaced raw JSON payload dump with structured formatter details
- No new dependencies; pure additive change to existing interface

## Quality Metrics
- **Tests Passing:** 671 (all game formatters covered)
- **Build Status:** ✅ Clean
- **Lint Status:** ✅ Clean

## Decision Document
Logged to `.squad/decisions/inbox/gately-p64-stats.md`
