# Orchestration Log: Ortho — P6.3 Formatters

**Date:** 2026-03-19T14:23:20Z  
**Agent:** Ortho (Frontend Dev)  
**Focus:** P6.3 formatters for Backgammon, Dominos, and Risk

## Work Summary

Added three game-specific `MoveFormatter` implementations to `client/src/ui/historyFormatters.ts`:

- **backgammonFormatter** — Handles `roll`, `move` (with point 1-24 display, Bar, Off), and `pass` actions
- **dominosFormatter** — Handles `play` (with graceful pip detection), `draw`, and `pass` actions
- **riskFormatter** — Handles `pickTerritory`, `placeArmy`, `attack`, `captureMove`, `fortify`, `tradeCards`, and `endPhase` actions; resolves territory IDs to display names

## Results

- All action types covered with emoji icons and graceful fallbacks
- 580 tests passing
- No regressions
- Formatters are future-proof for server-side enrichment

## Design Highlights

- Dominos pip detection defers to future server enrichment; falls back to entry description
- Risk uses existing `getTerritoryById()` shared function; no data duplication
- Attack display limited to dice count (server-side results not in client payload)

## Files Modified

- `client/src/ui/historyFormatters.ts`

## Decision

Decision recorded in `.squad/decisions/inbox/ortho-p63-formatters.md` (to be merged into decisions.md).
